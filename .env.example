# Required — set as a secret in Railway's dashboard, never commit the real value.
ANTHROPIC_API_KEY=

# Optional — deployment configuration.
AETHERIS_TOOLS_CONFIG=config/example_tools.json
AETHERIS_DEMO_TENANT_ID=tenant-demo
AETHERIS_TOOL_DOMAIN_ALLOWLIST=api.tenant-partner.example.com
PORT=8000

# Secrets referenced by config/example_tools.json's tool definitions.
PARTNER_API_TOKEN=
PARTNER_API_KEY=
